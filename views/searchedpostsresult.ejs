<!doctype html>
<html>
  <head>
    <title>Welcome to <%=forumName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Mango Forums Posts that match your search '<%=query%>':</h1>
    <ul>
        <!-- Create a for loop to go through all topics array, if we have no topics then display the message of failed to find-->
        <% if(postsData.length == 0) { %>
          <li>          
            Seems like we couldn't find a match :(
        </li>
        <% } %>
        <% for (let i = 0; i < postsData.length; i++) { %>
            <li>          
                <!-- At each index get the name and creator and put join them in the list element-->
                <strong><%= postsData[i].description %></strong> -> Posted By <%= postsData[i].creatorUsername %> -> in <%= postsData[i].topicName %>
            </li>
        <% } %>
    </ul>
  </body>
</html>